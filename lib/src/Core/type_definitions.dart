/// The `FutureJsonType` typedef represents a future that resolves to a JSON object.
///
/// This typedef is defined as a `Future<Map<String, dynamic>>`, indicating that it represents a future that resolves to a JSON object.
typedef FutureJsonType = Future<Map<String, dynamic>>;

/// The `JsonType` typedef represents a JSON object.
///
/// This typedef is defined as a `Map<String, dynamic>`, indicating that it represents a JSON object with string keys and dynamic values.
typedef JsonType = Map<String, dynamic>;
